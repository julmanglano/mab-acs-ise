ALLMACS:
{%for item in IDGROUPS %}
{% for it in lookup ('vars', item.name) %}
{% if it.id == 0 %}
- mac: {{ it.mac }}
  description: "{{ it.description }}"
  idgroup: {{ it.idgroup }}
  enabled: {{ it.enabled }}
  id: {{ it.id }}
{% endif %}
{% endfor %}
{% endfor %}